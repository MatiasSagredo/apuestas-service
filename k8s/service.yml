apiVersion: v1
kind: Service
metadata:
  name: apuestas-service
  namespace: vidalcasino
spec:
  type: ClusterIP
  selector:
    app: apuestas-service
  ports:
    - port: 8005
      targetPort: 8005
